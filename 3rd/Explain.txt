プログラミングII 第3回レポート
学籍番号：37714091
氏名：西村健太

以下，説明

[各ファイルの説明]
・meikai12-1
(Car.java) 自動車クラス【第２版】教科書ソースコードにより作成。
(Day.java) 日付クラスDay【第３版】教科書ソースコードにより作成。
(OdometerCar.java)  Carクラスを派生させたOdometerCarクラスを記述。総走行距離を表すmileageフィールドを新たに定義し、Carクラスの初期化に必要な引数に加え、初期の走行距離を受け取るようにした。
moveメソッドをオーバーライドさせ、親クラスのmoveメソッドを呼び出して移動判定を行い、動くことができた場合にその距離をmileageに足し合わせている。
また、総走行距離を得るためにgetMileage()メソッドも記述した。
(OdometerCarTester.java) 作成したOdometerCarクラスをテストできるように記述。最初にputSpec()を記述し、問題なくメソッドが継承されているかの確認を行わせる。
続いて、2回の移動を行わせる。最初は移動可能な距離、最後は移動できない距離を設定している。移動したタイミングがわかるような記述も出力させている。
移動(できなかった場合も)後は、aの走行距離と燃料を出力させるよう記述した。

・meikai12-2
(Account.java) 銀行口座クラス【第2版】教科書ソースコードより作成。また、39行目にgetTotalBalance()を記述した。
(TimeAccount.java) 定期預金付き銀行口座クラス【第1版】教科書ソースコードより作成。また、24行目にgetTotalBalance()を記述し、28行目にcompBalanceを記述した。
(TimeAccountTesterEx12.java) Accountクラスのa,bとTimeAccountクラスのc,dを作る。次に、Accountクラス同士、AccountクラスとTimeAccountクラス、TimeAccountクラス同士のすべてのパターンで、クラスメソッドcompBalanceが正しく動くかを確認させる。

・kadai1
(LunchA.java) 雛形の???を適切に埋めた。コンストラクタの記述のみだった。
(LunchB.java) LunchAの派生クラスのためextendsを入力。変数名は先のコードを見て適切に入力。コンストラクタはLunchAクラスにあるものはそのまま。メソッドを適切にオーバーライドさせて完了。
(LunchC.java) さらにLunchBから派生。要領は同上
(LunchTester.java) (4~14行目は間違えて自作したもの) スライドにあったものに加えて、クラスを生成する前、printメソッドを使用する前に、何を行ったかがわかるように記述を追加した。

・kadai2
(VirtPlotGraph.java) 親クラスであるVirtPlotGraphクラスを記述したファイルである。
4,5行目はY座標を設定しており、変更できないようfinalを用いている。9行目の???は、y=0の点を含めたchar型の配列にしたいため、Y_MAX - Y_MIN + 1と埋めた。
アクセッサを定義した後、14行目は、bufferの中身の要素すべてを空白にする処理で、.lengthを埋めてすべてが空白で埋まるようにした。
plot()の中身は、yがY_MINかY_MAXの範囲を出た時に自動的にその範囲に収めるようにした。buffer[y - Y_MIN]とすることで、添え字と実際のyの値を対応させた。
30行目でコンストラクタを定義し、アクセッサ、computeYメソッドを定義。
(SinVPGraph.java) VirtPlotGraphクラスの派生クラスであり、正弦曲線をプロットさせるクラス。引数として新たに周期をあらわすtが追加。コンストラクタを再定義し、 tのアクセッサもこれまで通り定義。
computeYメソッドに関しては、Math.sin,Math.PIを利用して計算した後、Y_MAXを用いて振幅を合わせ、intでキャストしてオーバーライドした。
(SawToothVPGraph.java) SinVPGraphクラスの派生クラスであり、のこぎり波関数をプロットさせるクラス。そのため、tのアクセッサは定義する必要はない。コンストラクタを再定義し、computeYメソッドは、yをx % tにした後、倍率を合わせるために13行目の処理を行っている。その後、Y_MAXを超えたyに関しては、Y_MAX*2をひくことで平行移動させて、のこぎり波関数を実現させた。
(VirtPlotGraphTester.java) これまで作った3つのグラフをプロットするクラスをテストするクラス。もともと入っていた周期を変更した(変更は出力)

[結果の説明]
車高：1500mm
車長：4000mm
aの走行距離 = 0.0
aの燃料 = 10000.0
ーー移動ーー
aの走行距離 = 50.0
aの燃料 = 9950.0
ーー移動ーー
aの走行距離 = 50.0
aの燃料 = 9950.0

(putSpec()は正常に動作していることがわかる。また、移動が成功した後は総走行距離が増えており、getMileage()含め、新たに追加したmileageフィールドもうまく機能していることがわかる。)



java TimeAccountTesterEx12.java 
a vs b
1
a vs c
0
c vs d
-1
(Accountクラス同士、AccountクラスとTimeAccountクラス、TimeAccountクラス同士のすべてのパターンで正しく動作している)

java LunchTester      
ーーLunchAクラス("チキンカレー",450)を生成ーー
ーーLunchBクラス("お子様カレー",300,"エビフライ",200)を生成ーー
ーーLunchCクラス("激辛カレー",500,"はちみつ", 100,"野菜サラダ", 180)を生成ーー
ーーすべて.print()↓ーー
Order: 1
Menu: チキンカレー
Price: 450
Order: 2
Menu: エビフライお子様カレー
Price: 500
Order: 3
Menu: はちみつ激辛カレー野菜サラダセット
Price: 780
(どこで何が行われたかはーーーーで囲まれた記述の通りである。Orderは、生成された順に1,2,3となっているため正しいことがわかる。Priceも、注文したセットの金額をすべて足し合わせたものになっているため正しいことがわかる)


java VirtPlotGraphTester.java 
ーー直線(1)、正弦(2, xRange / 2)、のこぎり波(3, xRange / 3)を定義ーー
ーープロットーー
          3          
          1  3 2     
          1     3  2 
          1        23
   3      1    2     
      3   2          
     2    3          
 2        1  3       
 2        1     3    
     2    1         3
   3      2          
      3   1    2     
          3        2 
          1  3     2 
          1    23    
          2         3
   3 2    1          
 2    3   1          
 2        3          
     2    1  3       
          2     3    

(正弦曲線は正しく2周期分プロットされており、のこぎり波も3周期分プロットされている、intにキャストしている関係でY_MINに値がない場合もあるが、倍率も正しく反映できている。)

[考察]
TimeAccountクラスに関して、普通の預金と定期預金の合計を出すために、getBalance() + timeBalanceとしていたが、TimeAccountクラスの場合にしか正しく動かないため、新たにgetTotalBalance()を作成することで解決した。
SawToothVPGraphクラスのcomputeYメソッドの13行目は、最初にy *= Y_MAX / (t / 2);としていたが、これでは割り算を先にしてからyを掛けることになってしまう。それになかなか気づかずに苦戦した。

[自己評価]
B
都度explain.txtに記述したことで、これまでより丁寧に記述することができた。
時間にも余裕があったのでチャレンジ問題にも挑戦できるとなおよかった。